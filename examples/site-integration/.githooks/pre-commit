#!/usr/bin/env bash

echo "🔍 Validating front matter of staged files..."
bundle exec fmv-validate --staged
STATUS=$?

if [ $STATUS -ne 0 ]; then
  echo "❌ Commit blocked: fix the front matter issues above before committing."
  exit 1
fi
